{
  "version": "0.3.4",
  "dokployVersion": "0.30.2",
  "notes": "real disk accounting: per-service storage now shows honest numbers — data (volumes + layers, exclusive) and disk footprint (data + shared image) — on cards, details and a new debug disk section with a full breakdown (volumes / images / build cache / logs residual) and top consumers. mit license added."
}
