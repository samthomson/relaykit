{
  "name": "relaykit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn dev --host\"",
    "start": "cd backend && yarn start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "playwright": "^1.58.2"
  },
  "dependencies": {
    "nostr-tools": "^2.23.3"
  }
}
